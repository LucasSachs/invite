import { Baloo_2 as Baloo2, Leckerli_One as LeckerliOne, Open_Sans as OpenSans } from 'next/font/google'

export const leckerliOne = LeckerliOne({
  weight: ['400'],
  subsets: ['latin'],
})

export const baloo2 = Baloo2({
  weight: ['700'],
  subsets: ['latin'],
})

export const openSans = OpenSans({
  weight: ['400', '600'],
  subsets: ['latin'],
})
